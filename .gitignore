node_modules/
.env
.env.*
*.log

# File thiết kế gốc của khách (dùng để dựng template) — không đưa lên git
**/templates-src/

# Ảnh đối chiếu khi soát template — chỉ dùng trên máy
**/public/templates/**/verify/
